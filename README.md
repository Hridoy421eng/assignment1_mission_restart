1. When  the variable get declared without the value it returns undefined.... and when the coder intetionally leaves the variable empty it  returns null.
2. map() works on every value of an array and returns an array on the otherhand,  forEach doesn't return something.
3. == compare the value only and if needed then convert the type ... And === compare both value and type on the same time and don't convert anything.
4. async/await makes the api call easy. It makes the error handling easier.
5. If the variable is globally declared (menas outside the function or block)  , it is accessible from everywhere. If it is declared functionally (inside the function ), it is only acceassaible on that function only, we can't find it outside the function. If variable
is declared inside the block it only works inside it. But if we declare it using var it dosn't follow block scooop, it jumps over to functional scoop. 
